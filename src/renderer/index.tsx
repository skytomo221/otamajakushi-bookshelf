import React from 'react';
import ReactDOM from 'react-dom';

import BookshelfForm from './components/BookshelfForm';

import '../../index.css';

const container = document.getElementById('contents');

ReactDOM.render(<BookshelfForm />, container);
